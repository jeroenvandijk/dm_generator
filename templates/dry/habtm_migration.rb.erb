class <%= migration_name %> < ActiveRecord::Migration
	def self.up
		create_table :<%= habtm_table_name %>, :force => true do |t|
			t.belongs_to :<%= habtm_pair.first %>
			t.belongs_to :<%= habtm_pair.second %>
		end
	end

  def self.down
    drop_table :<%= habtm_table_name %>
  end
end