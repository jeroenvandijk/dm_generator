%h1= translate_for "<%= singular_name %>.edit.title"

= render :partial => "form", :locals => {:<%= singular_name %> => @<%= singular_name %>}

= link_to translate_for("<%= singular_name %>.edit.link_to_show"), <%= path_for(:show) %>
|
= link_to translate_for("<%= singular_name %>.edit.link_to_index"), <%= path_for(:index) %>
